// ===== Module 9 — Oplossingen (hulpbestand: default export) =====

// Oefening 3: één default-export per bestand (het "hoofd-ding").
export default function begroet(naam: string): string {
  return `Hallo, ${naam}!`;
}
